<%= form_for(@task) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
   Skill : <%= f.select :skills, options_for_select(["skill1","skill2","skill3"]) %>
  </div>
  
 <div class="field">
  Priority : <%= f.select :priority, options_for_select(["High","Low"]) %>
  </div>
 
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
